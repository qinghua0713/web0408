###    js数据类型？

```
 Number boolean string Array    undefiend null

   null和undefined的区别？
 undefined和null在if语句中，都会被自动转为false，相等运算符甚至直接报告两者相等
```



###   运算符的种类？

```
?      1.算术运算符 2.赋值运算符 3.比较运算符 4.位运算符 5.逻辑运算符 6.成员运算符 7.身份运算符

   var a = 10;var b = a++; a、b最后的结果是？
     console.log(a,b)//结果为11

   “==”与“===”的区别？
   ==比较数值相等就为true ===比较数值相等，类型也要相等
```




    console.log(0.1+0.2 == 0.3)。//false

 

```
  NaN会在什么样的情况下出现呢？列举出现这种情况的代码。
     当你获取的元素不是数字类型的时候就会出现NaN 

  ?列举三种强制类型转换和2种隐式类型转换。
      三种强制类型转换：String()，Boolean()，Number()；
      二种隐式类型：+和-
```

?     


